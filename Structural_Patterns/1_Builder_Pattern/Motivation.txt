Motivation : When an object requires to be created piecewise then builder template is used.

In Pro words : When piecewise object creation is complicated provide an API for doing it succinctly 


When to identify when to use Builder:

When the constructor have a lots of data types as dependency and sometimes they are not required.
